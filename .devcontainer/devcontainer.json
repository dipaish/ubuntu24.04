{
    "name": "Ubuntu Dev Container",
    "build": {
      "dockerfile": "Dockerfile",
      "context": "."
    },
    "remoteUser": "root",
    "settings": {
      // Customize VS Code settings (for example, set the terminal shell)
      "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
      // List any VS Code extensions you’d like pre-installed
      "ms-python.python",
      "esbenp.prettier-vscode"
    ],
    "postCreateCommand": "echo 'Welcome to your Ubuntu Codespace! You are logged in as Root so no sudo required'"
  }
  